dist: trusty

language: node_js
node_js:
  - "10"

addons:
  chrome: stable
cache:
  directories:
    - ./node_modules

install:
- npm install
- npm install karma-firefox-launcher --save-dev

script:
  - npm run test:unit
  - npm run e2e
